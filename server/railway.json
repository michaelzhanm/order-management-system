# Railway 部署配置
# 在 Railway 中，/data 目录是持久化存储
# 详见 https://docs.railway.app/deploy/dockerfiles

# 也可以使用 Nixpacks 自动检测（推荐）
# 此文件用于指定 Dockerfile 路径或启动命令

# 使用 Nixpacks 自动检测（推荐）
# Railway 会自动识别 Node.js 项目并安装依赖

# 为 better-sqlite3 指定构建包
# Nixpacks 会自动处理原生模块编译
